apiVersion: v1
kind: ResourceQuota
metadata:
  name: lab4-quota
  namespace: lab4
spec:
  hard:
    pods: "10"
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
