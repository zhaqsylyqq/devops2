apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-webapp-allow-http
spec:
  podSelector:
    matchLabels:
      app: {{ .Release.Name }}-webapp
  policyTypes:
  - Ingress
  ingress:
  - ports:
    - protocol: TCP
      port: 80
